# <Название возможности>

- **Идентификатор**: `<capability-id>`
- **Статус описания**: `<подтверждено | частично подтверждено>`
- **Последняя проверка**: `<YYYY-MM-DD>`
- **Bead**: `<issue-id>`

## Назначение

Кратко опишите ценность возможности и пользователя, для которого она
существует. Фиксируйте только актуальное наблюдаемое поведение.

## Наблюдаемое поведение

Опишите последовательность действий и результат, доступные в текущем состоянии
репозитория. Не смешивайте существование материала, заявленный статус и
фактическое выполнение.

## Входные условия

- `<подтверждённая предпосылка>`

## Сценарии

### Сценарий: <краткое название>

1. **Дано**: `<подтверждённое исходное состояние>`.
2. **Когда**: `<наблюдаемое действие>`.
3. **Тогда**: `<проверяемый результат>`.

## Подтверждающие evidence

| Утверждение | Источник | Что подтверждено | Что не подтверждено |
| --- | --- | --- | --- |
| `<утверждение>` | `<относительный путь или команда проверки>` | `<граница доказанности>` | `<явное ограничение>` |

Для notebook отдельно укажите:

- существует ли файл в Git;
- какой статус заявлен в `README.md`;
- было ли воспроизводимое выполнение и чем оно подтверждено.

## Ограничения и нецели

- `<что возможность не обещает>`
- `<какие runtime, data или внешние системы не проверялись>`

## Критерии приёмки

- [ ] Все человекочитаемые пояснения написаны по-русски.
- [ ] Literal technical identifiers и точные пути сохранены без перевода.
- [ ] Каждое существенное утверждение связано с проверяемым evidence.
- [ ] Непроверенное выполнение не представлено как выполненное.
- [ ] Ссылки относительные и рабочие.
- [ ] Diff ограничен согласованной областью.

## Управление изменениями

Material delta проходит цикл delta → approval → evidence → fold-forward →
accept и связывается с отдельным Bead. После accept этот файл обновляется как
цельное описание текущего состояния; changelog здесь не ведётся.
